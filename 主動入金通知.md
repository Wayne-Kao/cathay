sequenceDiagram
    autonumber

    actor User as 企業用戶
    actor OP as 作中/分行同仁
    participant GMB
    participant API as API Bank
    participant Kafka
    participant CL as Composite Layer
    participant BL as Base Layer
    participant SYS as 核心系統
    participant ERP as 用戶ERP

    %% 訂閱流程
    User ->> OP: 填寫紙本申請書
    OP ->> OP: 審核資料 / 收件

    OP ->> GMB: 建立訂閱申請
    activate GMB
    GMB ->> API: 呼叫訂閱電文
    activate API
    API ->> CL: 傳送訂閱申請
    activate CL
    CL ->> BL: 傳送訂閱申請
    activate BL
    BL ->> SYS: 建立訂閱資料
    activate SYS

    alt 建檔成功
        SYS -->> BL: Success
    else 建檔失敗
        SYS -->> BL: Fail
    end

    deactivate SYS
    BL -->> CL: 建檔結果
    deactivate BL
    CL -->> API: 建檔結果
    deactivate CL
    API -->> GMB: 建檔結果
    deactivate API
    GMB -->> OP: 顯示結果
    deactivate GMB

    %% 異動通知
    Note over SYS,ERP: 訂閱成功後持續通知

    SYS ->> BL: 出入金異動(Socket)
    BL ->> CL: 異動通知
    CL ->> Kafka: Publish Event
    Kafka -->> API: Consume Event

    API ->> GMB: 取得 AES Key
    GMB -->> API: 回傳 AES Key

    API ->> API: 資料加密

    loop Retry 最多3次
        API ->> ERP: 發送通知
        alt 成功
            ERP -->> API: Success
        else 失敗
            ERP -->> API: Fail
        end
    end

    opt 三次失敗
        API ->> API: 記錄失敗 / 待補送
    end
